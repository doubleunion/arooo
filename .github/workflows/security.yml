# Brakeman security scanner - DISABLED
#
# Brakeman fails on Ruby 3.1 + Rails 6.1 due to the Logger load-order issue
# (uninitialized constant ActiveSupport::LoggerThreadSafeLevel::Logger) and
# reports EOL warnings for both Ruby 3.1 and Rails 6.1. Since the org shuts
# down October 2026 and we won't be upgrading further, this check is disabled
# rather than papered over with --no-exit-on-error.
#
# name: brakeman
#
# on:
#   push:
#     branches:
#       - main
#   pull_request:
#     branches:
#       - main
#
# jobs:
#   brakeman:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Set up Ruby
#         uses: ruby/setup-ruby@v1
#         with:
#           bundler-cache: true
#       - name: brakeman
#         run: bundle exec brakeman -w2
